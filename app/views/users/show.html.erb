<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
  <% @user.projects.each do |project| %>
    <%= project.name %>
  <% end %>
</p>

<hr>

<p> Proyectos: </p>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
<% @projects.each do |project| %>
  <tr>
    <td><%= project.name %></td>
    <td><%= link_to 'Asignar', allocations_path(project_id: project.id, user_id: @user.id), method: :post  %></td>
  </tr>
<% end %>
</tbody>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
